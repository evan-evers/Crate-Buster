#ifndef SCRAP_H
#define SCRAP_H

/*
* Header file for scrap and scrap-related functions.
* Scrap is implemented as a particle, so it doesn't have its own struct.
*/

void initScrap(int x, int y);

#endif